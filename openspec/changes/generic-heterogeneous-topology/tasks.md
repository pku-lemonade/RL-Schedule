## 1. Canonical graph contract and independent baseline

- [ ] 1.1 Establish the pre-change baseline in `.venv`: run the detailed unittest suite, strict Pyright with `--pythonpath .venv/bin/python`, and scoped lint; record results/environment in `simulator_detailed/docs/topology.md`. Capture compact independent mesh edge and representative timing/trace expectations from the committed baseline, including custom fabrics/ports and paired link slowdown semantics; verify these are not computed through the new adapter.
- [ ] 1.2 Add strict version-one canonical graph records in `simulator_detailed/configs/schemas/topology.py` for tiles, fabric routers, independent directed link ports, attachments, compute selection, resources, origin and completeness; verify valid/invalid versions, IDs, references, port collisions, parallel edges, availability and worker mappings in `tests/test_topology.py`.
- [ ] 1.3 Add normalization, structural hashing, export and stable canonical-to-runtime maps in `simulator_detailed/topology.py`; verify reordered input/path independence, fabric-qualified identity, nonnumeric IDs, wrap/disabled metadata, unique byte capacity and explicit unknown connectivity against hand-authored fixtures in `tests/fixtures/topology_reference.json`.
- [ ] 1.4 Add the new production modules to `simulator_detailed/pyrightconfig.phase2.json`; run focused topology tests, strict types and scoped lint, update evidence/task boxes, review `git diff --check`, and commit this completed graph-contract part before section 2.

## 2. Profile and legacy adapters

- [ ] 2.1 Implement profile inventory projection in `simulator_detailed/topology.py` using child-1 normalized data and provenance; verify the real Wormhole example's 120 tiles, 240 fabric routers, 240 physical attachments, 72 selected workers and 86 unique resources against the independent child-1 fixture, with unresolved links/undeclared attachment permissions, no invented local ports or availability, and no new execution capability.
- [ ] 2.2 Implement the legacy mesh adapter with current row-major router IDs, per-fabric directed link order, physical coordinates, PE bindings and typed DMA bindings; verify 1x1, 3x2, 4x4 and custom-fabric/local-port cases against the retained independent baseline, including 48 directed links per 4x4 fabric.
- [ ] 2.3 Make `simulator_detailed/endpoint_registry.py` consume explicit graph endpoint bindings while retaining its legacy constructor; verify memory/transit/disabled-compute inventory creates no implicit PE or DMA endpoint, and existing DMA mode, fabric and local-port collision tests still pass.
- [ ] 2.4 Introduce shared graph ownership in `simulator_detailed/architecture.py` without accepting heterogeneous DFG/profile execution; verify legacy core/mapper identities and all child-1 early profile gates remain intact, and graph/replay documents are rejected clearly by legacy architecture entry points.
- [ ] 2.5 Run focused graph/profile/registry tests plus the existing NoC/DMA suites, relevant strict types and lint; record adapter parity and remaining representation-only limits, then commit this completed adapter part before section 3.

## 3. Directed bindings and validated routing policies

- [ ] 3.1 Add the routing policy boundary and canonical-map-based legacy XY policy in `simulator_detailed/routing.py`; verify representative and all-pairs small-mesh routes match independent XY expectations, including same-router delivery and unavailable output rejection.
- [ ] 3.2 Refactor `simulator_detailed/noc.py` to bind input/output links independently and build enabled directed edges from the canonical graph; keep mesh/paired-binding compatibility wrappers and single-VC arbitration/credit behavior. Verify input-only/output-only ports, parallel edges, missing reverse links, atomic binding validation, legacy edge/trace order and packet/burst/backpressure regressions.
- [ ] 3.3 Complete graph-driven `Arch` NoC/core attachment construction and preserve paired link/router slowdown helpers; verify configured PE/DMA bindings resolve the same runtime objects, existing service behavior is unchanged, and baseline slowdown/recovery affects the same directions at the same modeled times.
- [ ] 3.4 Add explicit graph transport context to resolved replay endpoint records and `simulator_detailed/utils/definitions.py` flits, with no fabricated mesh geometry; wire context checks into link/router admission. Verify legacy Message/DMA behavior and FIXPATH rejection, wrong graph/plan/fabric/format/endpoint rejection, and unchanged credits/queues/grants on rejected admission in `tests/test_topology_transport.py`.
- [ ] 3.5 Implement explicit unicast route compilation and union channel-dependency validation in `simulator_detailed/routing.py`; verify directed continuity, same-router routes, missing/disabled edges, source/destination ports, no repeated routers, undeclared reverse routes, and a cycle formed by individually simple routes. Review the actual grant/credit resource dependencies against the design's drain argument before enabling this policy.
- [ ] 3.6 Include routing/context changes in strict typing; run the relevant new tests and all legacy NoC/DMA regressions plus scoped lint, record route and timing parity evidence, and commit this completed directed-transport part before section 4.

## 4. Executable heterogeneous replay and trace export

- [ ] 4.1 Add the versioned replay/result models to `simulator_detailed/configs/schemas/topology.py`: graph reference, explicit routes, fully resolved configurable transport settings, traffic, finite sink service and cycle limit. Verify units/clock references, overrides, unsupported fields/modes, mixed document kinds, graph completeness and all pre-construction admission errors.
- [ ] 4.2 Add `simulator_detailed/transport.py` to construct the graph's enabled routers, links and explicit replay terminals using shared transport machinery; verify non-grid traffic, same-router delivery, exact payload/physical-byte accounting and transit through a disabled worker router without creating its compute endpoint, plus two fabrics sharing resource inventory without memory service.
- [ ] 4.3 Implement completion/drain accounting and bounded unsuccessful-run reporting; verify multi-flow contention with small buffers/slower independent sinks conserves credits/bytes and drains, and timeout or idle-with-pending cases return incomplete status with pending identities. Compare isolated-link costs under two widths/clocks and packet boundaries to independently calculated expectations.
- [ ] 4.4 Add versioned graph/plan exports and replay trace integration in `simulator_detailed/transport.py` and, where shared helpers are needed, `tracing.py`; verify every event resolves to actual runtime router/link/attachment objects, fabric-local index collisions remain distinct, disabled/uninstantiated entries retain metadata, and legacy trace JSON stays compatible.
- [ ] 4.5 Add `simulator_detailed/replay_topology.py`, `configs/topologies/heterogeneous_example.json` and `configs/replays/heterogeneous_unicast.json`; verify the three inspection/replay commands in `design.md`, relative graph paths, pure JSON stdout, failure exit codes and no result files for invalid admission. Keep generated traces in temporary validation directories rather than committing them.
- [ ] 4.6 Run graph/replay and legacy regression tests, strict types covering new runtime/CLI modules and scoped lint; record example graph/plan identities and executable-versus-unsupported features, then commit this completed replay part before section 5.

## 5. Consumer compatibility and legacy graph views

- [ ] 5.1 Add dependency-free consumer contract checks in `simulator_detailed/topology_compatibility.py`; verify supported legacy graphs, unsupported event formats, unknown consumer names, same-count/different-role or different-directed-edge graphs, and spoofed compatibility labels before any feature/checkpoint work in `tests/test_topology_consumers.py`.
- [ ] 5.2 Refactor `simulator_detailed/predictor/topology.py` into a view of the canonical legacy graph; verify its public mesh constructor, flattened fabric/link indices and path-node ordering match the retained baseline without a second edge generator.
- [ ] 5.3 Wire graph/event contract validation into the detailed predictor builder/predictor boundary and `simulator_detailed/embedding/hw_encoder.py`, using canonical coordinates/identities for supported inputs; inspect actual guard call sites and verify supported feature widths/order and explicit incompatibility before tensor/checkpoint operations. Run real tensor smoke checks if Torch/PyG are available; otherwise record them as unavailable separately from passing offline guard tests.
- [ ] 5.4 Document detailed predictor 7-D and encoder 4-D contracts, current checkpoint limits, and unchanged top-level RL observation/action shapes in `simulator_detailed/docs/topology.md`; verify no model, top-level simulator/predictor/embedding/RL file or failure/workload dataset is modified, and no heterogeneous model support is advertised.
- [ ] 5.5 Run offline consumer/graph and impacted runtime tests, strict types for the new helper/view and scoped static/lint checks for changed consumer modules; record optional dependency limitations, then commit this completed consumer part before section 6.

## 6. Integrated evidence and handoff

- [ ] 6.1 Complete `simulator_detailed/docs/topology.md` and links from detailed configuration/README documentation; verify all published inspect/replay commands and compatibility examples, and map TR-G01..10 to actual tests, graph/plan hashes, environment and observed results. Distinguish inherited profile conformance from new synthetic runtime evidence and retain silicon timing as unvalidated.
- [ ] 6.2 Run `.venv/bin/python -m unittest discover -s simulator_detailed/tests`, `.venv/bin/python -m pyright --pythonpath .venv/bin/python --project simulator_detailed/pyrightconfig.phase2.json`, correctness Ruff on changed Python files and applicable Ruff rules on new modules; verify every new production module is covered and resolve only scoped findings. Repeat broader checks only after a relevant change/failure warrants it.
- [ ] 6.3 Inspect the real heterogeneous example's exported graph and completed replay trace against the independent fixture; verify exact directed/fabric identities, transit without compute, alias counts, finite resource drain, explicit profile/consumer rejection, and preserved legacy mesh/DMA/failure behavior. Report any unavailable ML/reference/hardware checks as unavailable rather than passed.
- [ ] 6.4 Reconcile delivered TR-01 and graph/attachment portions of HP-03/04 with the umbrella in the child evidence; verify all required child tasks have implementation evidence and keep Wormhole torus/failure policies, memory service, heterogeneous DFG execution and timing validation pending. Run `openspec validate generic-heterogeneous-topology --strict --no-interactive` and `git diff --check`, review the scoped diff, and commit the completed validation/documentation part before exploring `wormhole-dual-noc-routing`; keep push and spec sync/archive as separately requested actions.
