# Models

This directory keeps the small detector checkpoint used by the default workflow:

- `best_model.pth`: GNN fail-slow detector checkpoint.

Trained RL checkpoints are generated under `rl_agent/models/` and are ignored by Git.
